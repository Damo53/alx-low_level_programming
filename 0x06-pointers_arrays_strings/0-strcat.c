#include "main.h"
#include <stdio.h>
/**
 * _strcat - concatenate two strings
 * @dest: a string
 * @src: second string
 * Return: 0
 */
char *_strcat(char *dest, char *src)
{
	char cat;

	cat = dest && src;
	_putchar(cat);
	return (0);
}
